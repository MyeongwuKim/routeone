mutation SyncRouteArrivalNotificationInbox(
  $notifications: [RouteArrivalNotificationSyncInput!]!
) {
  syncRouteArrivalNotificationInbox(notifications: $notifications) {
    syncedCount
    notificationKeys
  }
}
