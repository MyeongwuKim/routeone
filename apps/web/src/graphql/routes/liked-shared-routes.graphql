query LikedSharedRoutes {
  likedRoutes {
    ...RouteSummaryFields
    stops {
      id
      place {
        contentId
        contentTypeId
        title
        address
        categoryLabel
        categoryName
        regionCode
        regionLabelKey
      }
    }
  }
}
