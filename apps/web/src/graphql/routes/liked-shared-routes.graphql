query LikedSharedRoutes {
  likedRoutes {
    ...RouteSummaryFields
    owner {
      ...SharedRouteOwnerFields
    }
    stops {
      id
      place {
        contentId
        contentTypeId
        title
        address
        categoryLabel
        categoryName
        regionCode
        regionLabelKey
      }
    }
  }
}
