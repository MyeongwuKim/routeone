mutation MarkRouteStopVisited(
  $stopId: ID!
  $visited: Boolean = true
  $verification: RouteStopVisitVerificationInput
) {
  markRouteStopVisited(
    stopId: $stopId
    visited: $visited
    verification: $verification
  ) {
    ...RouteDetailFields
  }
}
