mutation CompleteRouteStopVisit(
  $stopId: ID!
  $actualStayMinutes: Int
) {
  completeRouteStopVisit(
    stopId: $stopId
    actualStayMinutes: $actualStayMinutes
  ) {
    ...RouteDetailFields
  }
}
