mutation SetRouteStopVisitPhoto(
  $stopId: ID!
  $imageId: String!
  $imageUrl: String!
) {
  setRouteStopVisitPhoto(
    stopId: $stopId
    imageId: $imageId
    imageUrl: $imageUrl
  ) {
    ...RouteDetailFields
  }
}
