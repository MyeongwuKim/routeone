query SharedRoutes($regionCode: String, $limit: Int) {
  sharedRoutes(regionCode: $regionCode, limit: $limit) {
    ...RouteSummaryFields
    stops {
      id
      place {
        title
        categoryLabel
        categoryName
        regionCode
        regionLabelKey
      }
    }
  }
}
