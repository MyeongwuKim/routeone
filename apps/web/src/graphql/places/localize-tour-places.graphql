mutation LocalizeTourPlaces(
  $input: [TourPlaceLocalizationInput!]!
  $waitForFresh: Boolean = false
) {
  localizeTourPlaces(input: $input, waitForFresh: $waitForFresh) {
    contentId
    title
    address
    titleSource
    addressSource
    cached
  }
}
