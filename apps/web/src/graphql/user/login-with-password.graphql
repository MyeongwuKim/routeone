mutation LoginWithPassword($input: PasswordLoginInput!) {
  loginWithPassword(input: $input) {
    token
    user {
      id
      accountId
      email
      displayName
      avatarUrl
      authProviders
      locale
      createdAt
      updatedAt
    }
  }
}
