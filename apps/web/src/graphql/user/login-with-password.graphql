mutation LoginWithPassword($input: PasswordLoginInput!) {
  loginWithPassword(input: $input) {
    token
    user {
      id
      accountId
      email
      displayName
      locale
      createdAt
      updatedAt
    }
  }
}
