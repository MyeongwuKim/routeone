mutation RefreshAuthSession {
  refreshAuthSession {
    token
    user {
      id
      accountId
      email
      displayName
      avatarUrl
      authProviders
      locale
      role
      createdAt
      updatedAt
    }
  }
}
